project: werf-test-e2e-sbom-broken-pm
configVersion: 1
build:
  sbom:
    enable: true
    standard: cyclonedx@1.6
---
image: app
from: {{ env "BUILDER_BASE_IMAGE" }}
git:
  - add: /
    to: /
shell:
  beforeInstall:
    - printf 'this is not valid json at all, definitely broken output' > /pm.lock
packages:
  - type: os-pm
    workdir: /
