project: werf-test-e2e-sbom-type-change
configVersion: 1
build:
  sbom:
    enable: true
    standard: cyclonedx@1.6
---
image: app
from: {{ env "BUILDER_BASE_IMAGE" }}
git:
  - add: /
    to: /app
    stageDependencies:
      packages:
        - go.mod
shell:
  install:
    - cd /app && CGO_ENABLED=0 go build -o /bin/app .
packages:
  - type: go-mod
    workdir: /app
